<head>
    <link rel="stylesheet" href= "style.css" />
    <style>
        body {
            background-image: url("https://qph.fs.quoracdn.net/main-qimg-aca43bfd30a8ceaedaff4a598c0bdd42-c?_sm_byp=iVVdvf5s7TMT7rRM");
            text-align: center;
            
        }
    </style>
</head>

<body>
 
    <img id="gif" src="" />
    <p></p>
    <button onclick="getGiphyData()">I hate everything</button>
    
    
    <script>
        function getGiphyData(){
            // var searchTerm = document.querySelector('#userInput').value
            var searchTerm = 'ron swanson';
            var url='https://api.giphy.com/v1/gifs/search?api_key=JvJElIRJSzM67qtmqbEV3tlGZptX2Xa8&q='+searchTerm
            
            fetch(url)
            .then( data => {
              return data.json()
             })
              .then( json => {
                console.log(json)
                
                var arrayOfGifs = json.data
                var rand = Math.floor(Math.random() * arrayOfGifs.length)
                var firstGif = arrayOfGifs[rand].images.fixed_width.url
                document.querySelector("#gif").setAttribute('src', firstGif)
             })
            .catch( error => {
                console.log(error)
            })
        }
        getGiphyData()
    </script>
   
</body>